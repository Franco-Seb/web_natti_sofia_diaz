services:
  - type: web
    name: natti
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/nuts-web/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
